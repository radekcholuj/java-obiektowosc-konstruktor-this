public class Task {
    char[] title;
    char[] desc;
    boolean done;

    Task(char[] title, char[] desc, boolean done ){
        this.title = title;
        this.desc = desc;
        this.done = done;
    }
}
